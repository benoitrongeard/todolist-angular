import { CountTaskPipe } from './count-task.pipe';

describe('CountTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new CountTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
